public class MethodesTest {
    final static char PIERRENOIRE = 'N';
    final static char PIERREBLANCHE = 'B';
    final static char EMPLACEMENTVIDE = '*';
    public static void main(String[] args) {
  /*          /// Test de la methode Gobanrempli
            char[][] myBoard = new char[9][9];
            for (int i = 0; i < myBoard.length; i++) {
                for (int j = 0; j < myBoard[i].length; j++) {
                    myBoard[i][j] = EMPLACEMENTVIDE ;
                }
            }
            // Exemple de test
            char[][] plateaurempli = new char [9][9];
            for (int i = 0; i < plateaurempli.length; i++) {
                for (int j = 0; j < plateaurempli[i].length; j++) {
                    plateaurempli[i][j] = 'B';
                }
            }
            System.out.println("Le plateau est pas plein : " + MethodePlateau.GobanRempli(myBoard));
            System.out.println("Le plateau est PLEIN : " + MethodePlateau.GobanRempli(plateaurempli));
*/
        }
}
